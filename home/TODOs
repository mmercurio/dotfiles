
AeroSpace on macOS: current Alta (Option) hotkeys conflict with useful macOS shurtcuts. e.g. "extra" characters and symbols typically use Option key such as "degree" symbol: Option plus 0( ⌥ + 0).

macOS command line to configure "Click wallpaper to show Desktop" to be "never" or "only in Stage Manager" (which I do not use.)

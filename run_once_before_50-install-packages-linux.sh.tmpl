{{ if (eq .chezmoi.os "linux") -}}
{{ if (eq .chezmoi.osRelease.id "debian" "ubuntu") -}}
#!/usr/bin/env bash

set -eufo pipefail

packages=(
	curl
	jq
	net-tools
	openssh-server
	python3
	python3-venv
	tmux
	htop
	vim
	zsh
)

sudo apt update

sudo apt install -y ${packages[@]}

{{ end -}}
{{ end -}}
